import { ItemIdToStringPipe } from './item-id-to-string.pipe';

describe('ItemIdToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemIdToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
